#!/usr/bin/python3

"""
module defines a method is_kind_of_class
"""


def is_kind_of_class(obj, a_class):
    """
    checks if is instance of class or any inherited class in a_class
    """
    x_obj = isinstance(obj, a_class)
    if x:
        return True
    return False
